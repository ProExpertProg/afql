#user inputs query into streamlit text file
#query is parsed and accepted or rejected by the grammar
#search tuples, if extra processing is needed, do so and save the results
#annotate frames with tuples, merge frames into video, send video to streamlit 